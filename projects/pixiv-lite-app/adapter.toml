schema_version = 1
project_id = "pixiv-lite-app"
source_repo = "AloiceC/pixiv-lite-app"
handoff_ref = "ci/buildfarm"
manifest_path = "ci/buildfarm.toml"

# Public-safe delivery metadata. Fill with the owner's age public recipient
# before the first delivery build. The age private key never enters BuildFarm.
age_recipient = ""
